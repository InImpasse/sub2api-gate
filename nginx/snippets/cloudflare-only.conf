# cloudflare-source-geo.conf evaluates the original TCP peer retained in
# $realip_remote_addr. A plain allow/deny list here would instead evaluate the
# restored visitor address and reject every legitimate proxied request.
if ($cloudflare_source_allowed = 0) {
    return 444;
}
