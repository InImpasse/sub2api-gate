# Optional stronger origin authentication.
# Enable Authenticated Origin Pulls in Cloudflare, then install the matching CA here.

ssl_client_certificate /etc/nginx/ssl/cloudflare-origin-pull-ca.pem;
ssl_verify_client on;
