# Compatibility alias. The installer atomically manages the active per-hostname
# custom AOP stage; do not install or trust Cloudflare's shared Global AOP CA.
include /etc/nginx/snippets/sub2api-aop-active.conf;
